# OPSEC

```javascript
var h = opener.scorm;
h.set("cmi.score.raw", 83),
h.set("cmi.score.scaled", 83),
h.set("cmi.completion_status", "completed"),
h.set("cmi.success_status", "passed"),
h.set("cmi.exit", ""),
h.set("adl.nav.request", "exitAll"),
h.commit()
top.close();
```
